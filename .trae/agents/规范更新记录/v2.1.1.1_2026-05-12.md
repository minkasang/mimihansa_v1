# 规范更新记录 — v2.1.1.1

- **版本**：2.1.1.1（四段式；对应 `AGENT_SYSTEM_PROMPT.md` 文末）
- **最后更新日期**：2026-05-12

## 本次涉及文件

- `.trae/agents/AGENT_SYSTEM_PROMPT.md`

## 上一版（文末为 2.2）大致状态

- 版本号采用「主.次」两段小数式（2.1→2.2），小改也会很快耗尽次版本号。

## 本版（2.1.1.1）增加 / 修改内容

1. **规则 7.1**  
   - 改为 **四段式** `a.b.c.d`，每段 **0～99**，末位满 100 向左侧进位；默认日常改动只递增最右 `增补号`。  
   - 允许较大改动时左侧进位、右侧清零，并需在更新记录中写理由。  
   - 说明与历史 `v2.1`、`v2.2` 文件名仅作存档、不与新编号强行对齐。

2. **规则 7.2**  
   - 更新记录文件名示例改为 `v{a.b.c.d}_{YYYY-MM-DD}.md`。

3. **关键文件位置**  
   - `规范更新记录/` 注释改为 `vA.B.C.D_日期.md`。

4. **文末版本**  
   - 由 `2.2` 改为 **`2.1.1.1`**，作为采用四段式后的首个脚注（与旧 2.2 不作数值延续，以规则正文为准）。

## 待用户确认

- 无。
