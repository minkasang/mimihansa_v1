1. 对应版本号：v2.1.1.8，最后更新 2026-05-17
2. 改动章节/规则：
   - 项目背景：阶段更新为「阶段1+2+3已完成」
   - 关键文件位置：目录树新增 ModuleRegistry.ts、ModuleFactory.ts
3. 上一版 vs 本版：
   - 上一版 v2.1.1.7：阶段2完成（BrainEngine主循环）
   - 本版 v2.1.1.8：
     + 阶段3完成：ModuleRegistry指令模块注册表
     + 新增 ModuleRegistry.ts —— 通用模块注册+分派系统（替代switch-case）
     + 新增 ModuleFactory.ts —— 一次性注册18个内置模块（move/say/open_shop等）
     + 重构 scriptEngine.ts —— 删除140行switch-case，executeModule()委托给ModuleRegistry
     + 重构 BrainEngine.ts —— instructionToOutput()删除40行switch-case，委托给ModuleRegistry
     + main.ts —— 创建ModuleRegistry实例，注入ScriptEngine和BrainEngine
     + 架构收益：新增指令模块只需在ModuleFactory注册一次，全部引擎自动获得新能力
4. 争议点：无
