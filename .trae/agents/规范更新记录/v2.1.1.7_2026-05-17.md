1. 对应版本号：v2.1.1.7，最后更新 2026-05-17
2. 改动章节/规则：
   - 项目背景：阶段更新为「阶段1+2已完成」
   - 项目背景：更新已完成/待完成事项
   - 关键文件位置：目录树新增 engine/ 下6个文件 + Desires_Library.json
3. 上一版 vs 本版：
   - 上一版 v2.1.1.6：阶段1完成（NPC数据抽离）
   - 本版 v2.1.1.7：
     + 阶段2完成：BrainEngine主循环
     + 新增 ConditionEvaluator.ts —— 通用条件解析器，替代scriptEngine.ts中的70行if-else
     + 新增 Desires_Library.json —— 欲望与动机库（10条欲望定义）
     + 新增 DesireEvaluator.ts —— 从欲望库计算激活欲望列表
     + 新增 GOAPPlanner.ts —— 目标→指令序列拆解（支持休息/吃/休闲/社交/送礼/工作/购物）
     + 新增 InterruptChecker.ts —— 中断检测（生存威胁/天气/社交感知）
     + 新增 BrainEngine.ts —— 大脑主循环（串联全部管线：中断→需求→感知→欲望→目标→GOAP→执行）
     + main.ts 接入 BrainEngine：非商人NPC不再使用旧的3秒决策，改用每Tick大脑循环
     + main.ts 侧边栏显示NPC当前欲望状态
4. 争议点：无
