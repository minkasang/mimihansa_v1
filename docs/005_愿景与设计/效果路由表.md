# 效果路由表

> 运行时契约文档。统一所有 `{"key": value}` 效果到实际参数的映射。
> ⚠️ 任何系统写效果时必须查本表，用正确的参数名。

---

## 一、路由规则

```
效果格式：{"参数名": 变化值}

引擎读取后：
  1. 查本表 → 找到参数名对应的字段路径
  2. 是NPC参数？ → 写入 NPC.字段路径
  3. 是世界参数？ → 写入 World.字段路径
  4. 是本表未注册的key？ → 丢弃 + 日志警告
```

---

## 二、全部合法效果 key

### NPC 生理状态

| 效果 key | NPC字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"饥饿"` | `生理状态.饥饿度` | number | 负值=减少饥饿 |
| `"口渴"` | `生理状态.口渴度` | number | — |
| `"疲劳"` | `生理状态.疲劳度` | number | — |
| `"开心"` | `生理状态.开心值` | number | — |
| `"愤怒"` | `生理状态.愤怒值` | number | — |
| `"健康"` | `生理状态.健康值` | number | — |
| `"压力"` | `生理状态.压力值` | number | — |
| `"性欲"` | `生理状态.性欲值` | number | 0-100 |
| `"体力瞬时"` | `体力` | number | 瞬时体力消耗/恢复（追逐用） |

### NPC 物品/金钱

| 效果 key | NPC字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"物品栏"` | `物品栏` | "+物品id" / "-物品id" | 增减物品 |
| `"金钱"` | `金钱` | number | — |

### NPC 技能/后天气质

| 效果 key | NPC字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"书生气"` | `后天气质.书生气` | number | 每读一本书+0.1~0.3 |
| `"农业技能"` | `技能.种田` | number | 0-100 |
| `"锻造技能"` | `技能.锻造` | number | 0-100 |
| `"交易技能"` | `技能.交易` | number | 0-100 |
| `"建筑技能"` | `技能.建筑` | number | 0-100 |
| `"战斗技能"` | `技能.战斗` | number | 0-100 |

### NPC 战斗参数

| 效果 key | NPC字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"生命值"` | `战斗.HP` | number | 负值=扣血 |
| `"受伤等级"` | `战斗.受伤等级` | "轻"/"中"/"重" | — |

### NPC 关系/记忆

| 效果 key | NPC字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"好感度"` | `社会关系.好感度[目标ID]` | number | 需要附带 target |
| `"记忆"` | `记忆库[].事件` | string | 新增一条记忆 |

### NPC 怀孕/周期

| 效果 key | NPC字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"孕期"` | `周期.孕期天数` | number | 每日+1 |
| `"排卵延迟"` | `周期.排卵延迟` | number | — |
| `"流产"` | `周期.是否怀孕` | false | 直接置false |

### 动物

| 效果 key | Animal字段路径 | 类型 | 说明 |
|:---------|:-----------|:----|:-----|
| `"动物_健康"` | `生理状态.健康度` | number | — |
| `"动物_饥饿"` | `生理状态.饥饿度` | number | — |
| `"动物_所有者"` | `所有者ID` | string/null | — |
| `"动物_状态"` | `当前状态` | string | "圈养"/"自由"/"逃跑" |

### 世界（非NPC写入）

| 效果 key | 写入目标 | 类型 | 说明 |
|:---------|:--------|:----|:-----|
| `"格状态"` | `World.grid[x][y].状态` | string | 如 "耕地"、"已播种:小麦" |
| `"格踩踏度"` | `World.grid[x][y].踩踏度` | number | +/-值 |
| `"火堆燃料"` | `World.fire[id].燃料剩余` | number | — |
| `"建筑耐久"` | `World.buildings[id].耐久度` | number | — |
| `"建筑等级"` | `World.buildings[id].等级` | string | — |
| `"公示栏信息"` | `World.noticeBoards[id].信息列表` | "+条目" | — |
| `"人口计数"` | `World.人口` | number | +/-1 |

---

## 三、引擎读取本表的方式

```typescript
function applyEffect(effect: Record<string, any>, target: Entity, world: WorldState) {
  for (const [key, value] of Object.entries(effect)) {
    const route = EFFECT_ROUTING_TABLE[key];
    if (!route) { logWarning(`未知效果key: ${key}`); continue; }
    
    if (route.target === 'NPC') setNestedValue(target, route.path, value);
    else if (route.target === 'World') setNestedValue(world, route.path, value);
    else if (route.target === 'Animal') setNestedValue(target, route.path, value);
  }
}
```

---

## 四、与参数总表的一致性

本表所有 key 必须与 `NPC参数总表.md` 中的**参数名逐字一致**。
效果 key 使用中文短名（如 `"饥饿"` 而非 `"饥饿度"`），映射到实际字段路径（如 `生理状态.饥饿度`）的工作由本表完成。

---

*版本: 1.0*
*最后更新: 2026-05-23*
*状态: 初稿，运行时契约，后续加新效果必须在本表注册*
